// auto generated
import { MoLang } from "../../shared/molang.js";

export type ParticleParticleMotionParametricComponent = {
  /**
   * Directly set the position relative to the emitter. Evaluated every frame
   */
  relative_position?: Array<MoLang>;
  /**
   * Directly set the 3d direction of the particle. Doesn't affect direction if not specified. Evaluated every frame
   */
  direction?: Array<MoLang>;
  /**
   * Directly set the rotation of the particle. Evaluated every frame
   */
  rotation?: MoLang;
};
