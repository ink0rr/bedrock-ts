// auto generated
import { MoLang } from "../../shared/molang.js";

export type ParticleEmitterShapePointComponent = {
  /**
   * Specifies the offset from the emitter to emit the particles. Evaluated once per particle emitted
   */
  offset?: Array<MoLang>;
  /**
   * Specifies the direciton of particles. Evaluated once per particle emitted
   */
  direction?: Array<MoLang>;
};
