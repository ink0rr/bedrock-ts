// auto generated
import { MoLang } from "../../shared/molang.js";

export type ParticleParticleInitialSpinComponent = {
  /**
   * Specifies the initial rotation in degrees. Evaluated once
   */
  rotation?: MoLang;
  /**
   * Specifies the spin rate in degrees/second. Evaluated once
   */
  rotation_rate?: MoLang;
};
