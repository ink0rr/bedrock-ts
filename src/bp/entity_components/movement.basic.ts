/**
 * This component accents the movement of an entity.
 */
export type EntityMovementBasicComponent = {
  /**
   * The maximum number in degrees the mob can turn per tick.
   * @default 30
   */
  max_turn?: number;
};
