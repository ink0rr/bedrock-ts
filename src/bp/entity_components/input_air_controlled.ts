// auto generated
/**
 * When configured as a rideable entity, the entity will be controlled using WASD controls and mouse to move in three dimensions. Only available with 'use_beta_features' enabled and likely to be drastically changed or removed.
 */
export type EntityInputAirControlledComponent = {
  /**
   * Modifies speed going backwards.
   * @default 0.5
   */
  backwards_movement_modifier?: number;
  /**
   * Modifies the strafe speed.
   * @default 0.4
   */
  strafe_speed_modifier?: number;
};
