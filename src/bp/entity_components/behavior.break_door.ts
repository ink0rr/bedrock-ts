/**
 * Allows the actor to break doors assuming that that flags set up for the component to use in navigation.
 */
export type EntityBehaviorBreakDoorComponent = {
  priority?: number;
  speed_multiplier?: number;
};
