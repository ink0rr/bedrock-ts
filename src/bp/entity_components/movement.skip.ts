// auto generated
/**
 * This move control causes the mob to hop as it moves.
 */
export type EntityMovementSkipComponent = {
  /**
   * The maximum number in degrees the mob can turn per tick.
   * @default 30
   */
  max_turn?: number;
};
