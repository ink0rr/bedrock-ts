import { EventTrigger } from "../../shared/event_trigger.js";

/**
 * Adds a trigger that will run when a nearby entity of the same type as this entity becomes angry.
 */
export type EntityOnFriendlyAngerComponent = EventTrigger;
