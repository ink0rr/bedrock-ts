/**
 * Sets the entity's visual size.
 */
export type EntityScaleComponent = {
  /**
   * The value of the scale. 1.0 means the entity will appear at the scale they are defined in their model. Higher numbers make the entity bigger.
   * @default 1
   */
  value?: number;
};
