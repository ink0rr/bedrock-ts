/**
 * Allows the mob to go into stone blocks like Silverfish do. Currently it can only be used by Silverfish.
 */
export type EntityBehaviorSilverfishMergeWithStoneComponent = {
  priority?: number;
};
