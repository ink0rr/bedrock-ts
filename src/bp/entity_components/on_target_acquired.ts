import { EventTriggerFiltered } from "../../shared/event_trigger.js";

/**
 * Adds a trigger to call when this entity finds a target.
 */
export type EntityOnTargetAcquiredComponent = EventTriggerFiltered;
