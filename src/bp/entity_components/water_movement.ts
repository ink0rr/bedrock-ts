/**
 * Sets the entity's movement speed in water.
 */
export type EntityWaterMovementComponent = {
  /**
   * Drag factor to determine movement speed when in water.
   * @default 0.8
   */
  drag_factor?: number;
};
