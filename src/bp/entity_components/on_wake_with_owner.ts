import { EventTrigger } from "../../shared/event_trigger.js";

/**
 * Adds a trigger to call when this pet's owner awakes after sleeping with the pet.
 */
export type EntityOnWakeWithOwnerComponent = EventTrigger;
