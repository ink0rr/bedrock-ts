/**
 * Transforms a block
 */
export type BlockTransformationComponent = {
  /**
   * The block's translation
   */
  translation?: Array<number>;
  /**
   * The block's scale
   */
  scale?: Array<number>;
  /**
   * The block's rotation
   */
  rotation?: Array<number>;
};
